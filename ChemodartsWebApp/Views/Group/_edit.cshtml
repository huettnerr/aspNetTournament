@model GroupFactoryEdit

@using (Html.BeginForm("Edit", "Group", FormMethod.Post))
{
    @Html.ValidationSummary(true)

    @Html.EditorFor(m => Model)
    <button type="submit" class="btn btn-primary">Save</button>        
}